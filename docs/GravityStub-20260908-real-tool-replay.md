# GravityStub 单样本旧工具回执离线实验
## 范围
样本：DebugLog/chat/2026-09-08/chat-msg_1788836272272_assistant_3y6x2pe-110604_302-6b7a.json
样本SHA256：6ab0bf8fda24ba0b76ffc79e9017d3b1c9ab6dfd327a3530d9a536a6e3cf508f
十二个请求记录；非system历史在此样本逐轮前缀不变。只读取记录，不执行历史命令、不调用模型或embedding、不打开数据库、不修改生产发送、配置或history.json、不重启、不提交推送。

## 脚本
- scripts/inspect-gravity-tool-batches-20260908.cjs
- scripts/replay-gravity-tool-batches-20260908.cjs
- scripts/inspect-gravity-middle-evidence-20260908.cjs
均已通过独立语法检查及实际运行。未在本阶段重新运行256项联合回归。
第一次文档写入被递归上限拦截，后续核验NOT_FOUND，本次重新创建。

## 回放规则与结果
复用ToolCallParser和gravityOriginalStore。只在已检查样本中按批次位置关联，不代表通用可信配对。
全部命令、Assistant说明、System、真实用户指令保留。
最近两批回执保留。较早、可解析纯文本JSON、长度至少2000的回执才进入实验。Archery、markHistory、额外结构及无法解析的回执保留。
只保留字面头600和尾400及存根线索；成功状态unknown，不猜测工具是否成功。
第5索引回执非JSON或已截断，保留。
十二记录累计messages数组JSON序列化UTF-16代码单元：
baseline=1714169；folded=1206709；减少507460，即29.6038488620%。
最后两轮指定展开同一旧回执后=1244951；增加38242；净减少469218，即27.3729136392%。
32次精确恢复断言通过；样本文件前后哈希一致；未折叠内容和最近两批不变；finally原文仓关闭并清空。
不是Token或实际网络流量测量。未独立确认所有日志记录均实际发送。
恢复只针对日志已有原文，不包括日志产生前已截掉的工具输出。
恢复开销只含同次记录内重新展开，未计额外模型/工具轮次。

## 六个事实探针审计
按既有回放的固定折叠索引集，在源回执首次被省略的那轮检查；不用未来消息。
store-entry-limit、restore-checks-current-history、targeted-test-count、epa-fallback-depth：保留上下文无所选字面或归一化匹配。
sample-baseline-number、sample-projected-number：仅保留Assistant复述，原工具结果依据已省略。
六项均能通过原文仓精确恢复源回执及探针文本，审计退出0。
严格限制：
- 无匹配不等于事实语义上完全消失，例如“# tests 17”和“17/17”没有做语义等价匹配。
- 并未证明这些事实在当时后续任务中实际必需。
- Assistant复述不等于原始执行证据，但可作为已有结论线索。
- 未验证模型识别信息不足、主动取回或任务正确率。
- 此审计是证据可见性检查，不是任务保真验收。

## 结论及下一步
旧工具回执是有效压缩对象；此前本轮所有回执免疫过宽。
年龄规则加头尾只能作为离线基线，不能冒充语义引力场或生产安全策略。
本次有结束条件的单样本实验至此收口，不继续扩展探针/框架。
下一项应验证任务驱动的恢复行为：普通推进能否使用留存结论，需要核对命令/源码/测试时能否定位并恢复原批次；明确标注新设核对任务而非历史真实依赖。
生产恢复路径、语义候选选择、双Handler真实投影均未完成。## 后续验收：独立Agent恢复行为与同请求HTTP恢复循环

### 独立Agent受控案例
第一阶段委托 aa-delegation-1788849997488-489182fe 返回具体恢复句柄，主持人校验其唯一对应快照第13索引测试回执；但未返回约定的完整恢复JSON，第一阶段完整性及记忆污染声明不齐。
第二阶段委托 aa-delegation-1788850249407-e2d3b6b9 已完成，状态Succeed。回填后正确引用原始汇总：
- 定向：tests=17、pass=17、fail=0、duration_ms=172.5328。
- 联合：tests=256、pass=256、fail=0、duration_ms=3641.2347。
验证Agent声明输入完整、未发现具体答案的记忆污染；这些是自述，不是独立传输审计。
两阶段没有复用session_id，主持人根据固定日志转接，不是同会话生产恢复。
原消息序列化5353代码单元，存根1424，原位置替换增量3929；不等于总模型输入成本。
归档及占位符回显曾带出可执行工具请求，造成主对话重复读取；不应再次直接复制包含工具块的报告，不应将重复回显当新实验样本。

### 同请求HTTP恢复循环
新增 tests/gravityRestoreLoopHttp.test.js。
使用完整、未改源码的双Handler、现有ToolCallParser、本地HTTP模拟上游及gravityOriginalStore。
投影器和toolExecutor均为测试注入；未经过生产safeGravityProjection真实放行或生产ToolExecutor分发。
流程：初始全量请求 → FixtureTick → 旧回执存根到达模拟上游 → 模拟上游返回GravityRestoreFixture调用 → 请求局部原文仓回填 → 下次HTTP请求携带原文证据 → 最终回答。
保留全部历史命令及其对应位置，测试执行器只接受两个fixture工具，禁止历史命令重放。
原位置仍为存根，恢复证据通过新工具回执追加；未实现自动pin/原位置重展开。

覆盖双Handler各四项：
- 正常恢复：回填文本与原回执完全一致。
- 未知句柄拒绝。
- 另一仍存活原文仓的有效句柄拒绝，未泄露其正文。不是两个并发完整Handler之间的端到端恢复测试。
- 恢复期间Abort：原文仓关闭清空，不再请求上游，不输出最终答案。
检查源消息不变、普通fixture工具及恢复工具各执行一次、请求结束句柄失效；流式正常终态DONE仅一次。
请求局部原文仓由测试外层finally/response/abort释放，不代表生产已安装生命周期挂接。
首轮6/8通过；两项Abort失败来自注入投影器缺少真实safeGravityProjection的已中止透传契约，补齐后通过，未删掉终止及无后续fetch断言。

### 最新运行证据
- 独立node --check退出0。
- 专项8/8通过，fail/skipped/cancelled=0，TEST_EXIT=0，duration_ms=302.9335。
- 联合264/264通过，23个文件，fail/skipped/cancelled=0，REGRESSION_EXIT=0，duration_ms=4018.7538。
- 未执行本新增文件的独立空白差分验收，不宣称该项完成。
- 未修改生产Handler、工具注册或生产配置，未重启、未新增embedding、未改history.json、未提交推送。

### 接续边界
单个Agent案例支持恢复行为可行；模拟上游HTTP测试支持同请求回填机制可行。两项独立实验不能合并宣称真实模型生产端到端通过。
下一关键路径是将测试中的请求局部恢复适配整理为可复用且默认关闭的实现，并通过隔离Handler接入验证；不能直接打开生产折叠。
恢复入口必须绑定当前请求原文仓，只接受句柄，不接收文件路径或可执行命令；无仓、未知、跨请求及已关闭句柄拒绝。生产不得按测试脚本从任意日志兜底恢复。
语义候选选择仍未完成，年龄规则仅是离线比较基线，不替代原定语义引力场。## 当前状态快照（2026-09-08，用户要求记录）

### 目标与方向
GravityStub目标仍是减少工具循环重复发送上下文的成本：每轮依据当前用户任务和最新工具回执，从完整历史生成可逆投影，相关内容保留，暂不相关内容降为带线索及句柄的存根，需要时恢复。history.json不动。
优先复用VCP现有缓存、Memo特征与Rust几何能力；不是永久摘要替换，也不是另建通用检索或恢复平台。

### 本次方向纠正
用户明确指出：上上轮已执行的PowerShell调用和工具输出可以折叠。
此前按“本次循环全保护”“含协议/数字/路径整条保护”排除候选过宽，真实技术工作几乎没有候选。
修正原则：保护指令、协议对应关系、当前需要的事实与关键参数，不等于永久全量保留整个工具正文。
目前真实回放只折较早工具回执，所有命令和Assistant说明仍完整保留；命令正文折叠未实现。
年龄规则和头尾摘录仅是离线基线，不是最终语义引力场。

### 已完成及实证
1. 动态可逆投影隔离原型：每轮接收最新完整历史，追加保留句柄，历史正文/RAG/顺序变化使旧句柄失效；退出候选集合恢复原文，显式展开可固定。此前联合256项通过。
2. 单个真实十二请求日志离线回放：累计messages数组JSON序列化长度1714169降为1206709，减少29.60%；最后两轮指定展开同一旧回执后为1244951，净减少27.37%。单位UTF-16代码单元，不是Token或生产网络流量。32次精确恢复断言通过。
3. 六个事实探针：有的仅剩Assistant复述，有的没有所选字面匹配；源回执均可精确恢复。未证明这些事实当时必需，不能当作任务保真通过。
4. 微明两阶段受控案例：第一阶段请求正确句柄；第二阶段回填后正确引用定向17/17、fail=0、172.5328ms及联合256/256、fail=0、3641.2347ms的原始结果。两阶段无可复用session_id，由主持人转接；首阶段未返回完整约定JSON及完整性/污染声明。第二阶段未发现污染为Agent自述，不是独立审计。
5. 新增tests/gravityRestoreLoopHttp.test.js：完整双Handler、真实ToolCallParser、本地模拟HTTP上游，测试注入投影器和执行器。同请求存根发送→恢复工具调用→局部原文仓取回→新回执携带原文→续答。正常、未知句柄、跨仓句柄及Abort各覆盖流式和非流式。
6. 最新专项8/8、联合264/264通过，23个文件，fail/skipped/cancelled=0，退出0；联合耗时4018.7538ms。独立语法检查通过；新测试独立空白差分检查尚未完成。

### 仍未完成
- 生产可调用恢复工具、生产请求局部恢复适配器及生命周期接入。
- 根据用户目标与最新工具回执自动选择候选的语义决策。
- 本轮Memo L/R/S/Beta交接：已检查的向量交接包不含这些特征。RAG标签权重Beta不能直接作为聊天折叠阈值。
- Rust几何评分不等于省略许可；低残差不证明安全，高残差全保护也会留下无关历史。不伪造空间证据。
- HTTP恢复测试的证据通过新工具回执追加，未实现原位置自动展开/pin。
- 真实模型同会话生产端到端恢复、一般任务保真、真实Token收益及完整恢复轮次成本。
- 任意历史重排下的消息身份保护仍需验证。
- 生产实际折叠未启用，生产Token节省未验证；测试数不等于压缩质量。

### 下一步唯一范围
将现有HTTP测试中的请求局部恢复处理提取为最小、默认关闭的可复用适配器，让现有八项专项测试改用它。
复用现有原文仓；只接受绑定当前请求的句柄，不接收路径或命令，无仓/未知/跨请求/已关闭句柄拒绝。
不新增全局恢复服务、不建立第二套原文仓、不再做人工转接演示，不重复调查网关或扩展EPA框架。
该步骤不代表语义选择已完成，也不构成生产开启授权。

### 操作纪律与授权
本阶段未重启、未启用生产折叠、未主动新增embedding请求、未修改history.json、未提交推送Git。独立Agent调用确已发生，不能描述为完全离线无模型调用。
用户本次仅要求记录状态，不是新增开发、重启、生产折叠或embedding授权。
工作区有无关改动，禁止全量暂存覆盖。实际写入和测试状态以工具回执为准。
历史/委托回显曾含工具请求导致重复读取；后续只转述结果，不原样输出可执行工具块或已展开的大型异步报告。

### 主要交接文件
- docs/GravityStub-20260908-lifecycle-248-handoff.md（旧阶段）
- docs/GravityStub-20260908-dynamic-256-handoff.md
- docs/GravityStub-20260908-real-tool-replay.md（本文件，含最新264项阶段）