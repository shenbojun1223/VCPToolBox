# GravityStub 当前状态：内容选择阶段交接
日期：2026-09-08
用途：用户要求记录当前状态。接续以本文件及实际磁盘为准，不机械执行早期交接中的外围框架计划。

## 1. 核心目标与用户要求
减少多轮工具循环中旧历史反复全量发送的成本，同时保留当前任务需要的上下文。
组装期从完整原文重新投影，暂不需要的内容折叠，需要时恢复；不修改history.json。
用户多次指出过度设计，明确要求停止扩建恢复、生命周期、空间证明框架，优先完成“如何选择折叠内容”。
复用VCP已有可用能力，不以适配、测试数量或影子统计冒充实际效果。

## 2. 当前实际完成
### 恢复机制
- gravityOriginalStore、gravityRequestLifecycle、gravityReversibleSession已有隔离实现和测试。
- gravityRestoreAdapter默认关闭，绑定调用方既有原文仓，只接受句柄，不接受路径/命令。
- 双Handler HTTP恢复测试使用完整Handler、真实文本解析器、本地模拟上游；投影器和执行器仍为测试注入。
- 模型生产可调用恢复分发、生产生命周期和实际投影尚未接通。不可描述为生产端到端完成。

### 内容选择实验
modules/vcpLoop/gravityStub.js新增createGravityReceiptExperiment：
- 请求内绑定旧回执位置、正文指纹及交接向量。
- 每轮从完整历史重新投影；非system历史前缀变化清除旧向量记录。
- 最近两批回执保留，无向量或不支持的内容保留。
- 旧回执进入候选，不再因数字/路径整批排除。
- 当前仍为max(目标余弦,关注回执余弦)与默认0.35阈值的实验，不是最终策略。
- 存根标记检查由任意子串命中改为识别实验完整消息头，避免源码字面量导致整轮跳过。
- 最新修正：纯文件写入/编辑成功及特定校验跳过警告不改变关注中心，沿用最近实质回执；该实质回执缺向量则透传，不跨过它猜测。
- 警告仍原样保留，写入成功不等于语法/任务验证通过。
- 以上选择实验没有接入生产projectGravityStub；生产入口仍硬性返回原文。

## 3. 一次真实embedding授权及使用
用户明确授权一次有上限实验：最多24段、累计10万字符，分块计入，输入仅指定日志的用户目标和工具回执，不发送system。
实际发送13段、94593个UTF-16代码单元；1个目标、8批回执。
下一完整输入超过预算，停止于日志round 10之前；不截取部分冒充完整输入。
复用现有配置的endpoint和模型，独立脚本直接发送，禁用重试、备用和重定向；没有走完整生产RAG清洗/调用链。
向量及预算记录：
scripts/gravity-real-selection-20260908/
- started.json、budget.json
- vector-1/3/7/9/11/13/15/17/19.json
后续所有比较复用这些文件，无新增embedding请求。不可重跑--send；启动标记防止重复付费实验。
目标原文包括OneRing通知。真实回执输入为text parts拼接，不是生产RAG清洗文本。
实验构造交接包仅适配实验接口，不是真实生产RAG交接证据，也未验证向量空间兼容性。

## 4. 样本及证据边界
DebugLog/chat/2026-09-08/chat-msg_1788836272272_assistant_3y6x2pe-110604_302-6b7a.json
SHA256：6ab0bf8fda24ba0b76ffc79e9017d3b1c9ab6dfd327a3530d9a536a6e3cf508f
原日志12条记录。记录尾部有Assistant内容；回放重建截至最新回执的前缀，排除尾部Assistant，不能冒充已核实的线上实际请求。
真实向量选择回放覆盖8个有完整向量输入的前缀；不是全部12轮，其他实验可能采用不同前缀范围，不混算收益。
原始脚本最初有效轮数0是取错末条消息；首次离线回放后续轮跳过是存根字面量误判。两项已定位并修正，旧结果保留，不当有效语义负结果。

## 5. 已测结果
### 固定0.35阈值
replay-result-v2.json：
8个前缀baseline=1126602，projected=1126602，saved=0。
相关性约0.731～0.891，均未低于阈值。属于同一技术主题不等于每批全文都需重复发送。
没有折叠，所以该真实实验不能验证自动恢复。

### 相对排序＋候选正文保留预算
relative-comparison.json：
旧候选按相关性排序，设50%候选正文保留目标，完整保留跨过预算的项；不是严格总上下文硬上限。
8前缀baseline=1126602，projected=1017938，减少108664，约9.6453%。
单位为messages JSON序列化UTF-16代码单元，不是实际Token或生产网络节省。
发生两次自然排序导致的原文重新保留：round 8恢复index 9，round 9恢复index 3。
这证明排序变化可驱动恢复，不证明恢复恰好符合实际任务需要；恢复不是手工指定索引。
50%为实验预算，不是验证出的安全参数。
头尾省略暴露证据缺失：index 13的定向tests=17、pass=17、duration_ms=172.5328不在自身存根中，联合256项汇总尾部仍在。
相同“# fail 0”字面存在不能证明属于正确测试组；必须保护结果与执行批次/测试组的关联。
未证明当时后续任务实际依赖这些字段；这是证据可见性风险，不是已证实任务失败，也不是任务保真通过。

### VCP既有能力复用检查
- ResultDeduplicator：实际调用deduplicate，db=null、已有向量、原0.92阈值、不按路径合并、不靠maxResults强行删减。所有轮次零抑制候选；这条路线结束。
- Rust VexusIndex.computeOrthogonalProjection及EmbeddingUtils.cosineSimilarity：实际离线运行；原生分解有效，但扣除宽泛目标后的残差分数不能证明可省略。整套TagMemo/RiverMemo知识库管线未接入聊天选择。
- 原选择实验仍使用gravityStub内normalize/dot；审计复用公共余弦不等于选择器已替换。
- EPA/完整ResidualPyramid需要知识库标签、索引等数据，不为聊天复制完整依赖或伪造这些数据。

### 具体内容检查
- round 5低相关来自短写入确认，不是任务换题。确认策略修正后旧回执关注相似度约恢复至0.824和0.882。
- 文件名匹配只可作为保留线索，不能用未命中证明无依赖；同名文件不同版本也不能合并。
- 批内已有文件标题、元信息、正文parts，但目前识别仅样本启发式，不是生产可信结构元数据。
- 没有实现基于这些子块的最终选择；整批向量不能冒充子块向量。
- 最后逐字覆盖检查：21次旧文件正文出现检查，覆盖0；未投影，节省0。该分支已收口。
- 本质纠正：需治理的是跨请求重复发送，不一定是单个请求里重复出现两份。精确去重不是核心解法。

## 6. 最近验证状态
关注中心修正后：
- tests/gravityReceiptSelection.test.js 6/6通过，SELECTION_TEST_EXIT=0。
- 联合25文件276/276，fail/cancelled/skipped=0，4079.9911ms，REGRESSION_EXIT=0。
- gravityStub.js和上述测试独立node --check退出0。
- 这是隔离机制回归，不是语义质量、生产端到端或Token收益验收。
- 本阶段最新源码改动的独立完整差分验收未另行确认，不沿用此前270项阶段差分结论。

## 7. 当前结论与下一步
核心内容选择仍未完成，生产实际Token节省未验证。
停止继续试去重、残差、精确覆盖、阈值等旁路。此前已经在只读探查、重复读取与诊断脚本上投入过多，不能继续无限扩展。
回到已显示节省的“相关性排序＋预算”实验，解决具体缺陷：
1. 在压缩正文时保留执行状态、警告、结果证据及其工具批次/测试组归属，而不是整批只留头尾。
2. 结合当前具体操作对象和实质回执，避免短确认造成假切题。
3. 每轮从完整原文重选，重新需要时恢复。
4. 验证实际省略内容、任务质量与包含恢复成本的收益；不以恢复可用等同于模型一定会取回。
以上是下一方向，尚未实现，不是新的生产开启/重启/embedding授权。
不要追求用单个向量分数证明内容绝对无用；也不能凭预算强行省略后宣称安全。

## 8. 操作接续陷阱
- 本轮多次被工具递归上限拦截，拦截的请求未执行；接续先核对磁盘，不能假定写入成功。
- gravity-task-residual-check-20260908.cjs已执行；重复WriteFile产生gravity-task-residual-check-20260908(1).cjs，未运行，不应再重复实验。
- 曾准备gravity-tap-detail-replay-20260908.cjs，但写入请求被拦截；未核实创建，未执行，不宣称成功测试计时块压缩已落地。
- gravity-file-body-coverage-20260908.cjs首次写入被拦截，核验不存在后重新创建并执行，结果21次检查0覆盖。
- 有些回复错误重复“首次核对”已执行的工作；以后以实际回执和本交接为准。
- 旧回复把真实排序预算实验遗漏为“只有合成恢复验证”，应以本文件记录的真实9.65%比较及其风险为准。
- 文件工具outside-of-base-path警告代表校验跳过，独立node --check才作语法证据。
- 工作区有大量无关修改，不全量暂存、覆盖或清理。

## 9. 授权与运行边界
本阶段未重启、未开启生产折叠、未改history.json、未提交或推送Git。
只有前述一次真实embedding实验获授权并执行，后续没有新增请求。
当前停止自主循环并记录状态，不自动开启下一阶段。
主要文档：
- docs/GravityStub-20260908-restore-adapter-270-handoff.md
- docs/GravityStub-20260908-real-tool-replay.md
- 本文件为最新内容选择状态汇总。