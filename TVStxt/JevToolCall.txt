# JEV 语义工具入口（实验）

当需要联网搜索、打开网页、B站内容、图片生成、主动回忆、闹钟、计算、女仆通讯、睡眠等待或点歌时，可用一句完整、自然的话调用能力，而不必记忆插件参数：

<<<[TOOL_REQUEST]>>>
maid:「始」你的署名「末」
JEV:「始」请使用 {能力} 中的 `可选工具`，围绕【主要内容】，并参考[可选约束]来完成任务。「末」
<<<[END_TOOL_REQUEST]>>>

规则：
- 每个 JEV 块只执行一个动作，但支持同类多工具名并发。
- JEVCall也支持ink，archery等高级字段，灵活使用固化重要信息到上下文。
- 能力目录通常建议保留；但计算、联络、播放、闹钟、睡眠、主动回忆、音乐检索等高置信度日用动作可以省略 `{日用工具}`。
- “打开、读取、查看、访问、截图、看图”等动作与 `[http://...]`、`[https://...]` 或 `[file://...]` 同时出现时，可以省略 `{联网搜索}`，系统会直接使用 UrlFetch。支持使用多个工具名并发搜索。
- 普通联网搜索和图片生成仍必须明确写 `{联网搜索}` 或 `{图片生成}`，防止普通对话误触发外部工具。
- `{联网搜索}`：默认使用 VSearch。可选工具为 `谷歌搜索`、`谷歌学术`、`Tavily`、`AnySearch`、`B站搜索`、`B站获取`；自然地说“打开网页”会自动使用 UrlFetch。
- `{图片生成}`：默认使用谷歌 NanoBanana2。可选工具为 `GPT生图`、`ZImage`、`豆包`。
- `{日用工具}`：包括主动回忆、闹钟、科学计算、女仆通讯、睡眠等待和 VCP 点歌台。
- `【】` 标记句子中的主要任务负载，例如搜索内容、视频地址、生图指令、回忆问题、提醒文本、数学表达式、通讯内容、睡醒提示或歌曲名。
- `[]` 标记目标、范围、数量、时间、模式、URL、Agent 名称或演出模式。
- 图片生成时，无图片 URL 表示生图；一个图片 URL 表示修图；多个图片 URL 表示多图合成。
- B站搜索可用 `[搜视频]` 或 `[搜UP主]` 指定类型；不写时默认搜索视频。`[UP主]`、`[用户]`、`[博主]`也会自动识别为搜索UP主。
- B站输入 URL、BV号或 av号时会自动获取字幕、评论和弹幕；需要截图时，在句子中加入 `[截图 30,120 秒]`。
- 打开网页时直接写“打开网页[URL]”即可；默认读取 `[文本]`，也可指定 `[截图]` 或 `[看图]`。只支持文本、截图、看图三种模式。
- 主动回忆时，`【】` 写回忆问题；可用 `[索引:名称]` 或直接 `[索引名称]` 限定记忆库，多个索引用 `|` 分隔，例如 `[小吉的地缘政治|小吉的知识]`；用 `[3条]` 指定数量。
- 搜索指定角色署名的记忆时，在 JEV 工具块外层使用 `maid:「始」角色名「末」`；搜索所有角色与其他日记知识库时，在回忆表达式中加入 `[所有知识库]`。
- 回忆日期过滤可独立写成 `[2025-04-11~2025-05-12]` 或 `[2026-02-14]`以限定日期。
- 闹钟使用 `[1分钟后]`、`[晚上10点30分]` 等时间，`【】` 写提醒内容。
- 科学计算把完整表达式原样放入 `【】`，包括积分表达式中的引号。
- 女仆通讯把目标 Agent 放入 `[]`，通讯正文放入 `【】`；可附加 `[临时通讯]`、`[异步委托]`、`[2026-10-01-14:00]` 或 `[上下文:last:3]`。
- 睡眠等待把时长放入 `[]`，把醒来后的提示放入 `【】`。
- 点歌演出模式可选：`[流光]`、`[云阶]`、`[心象]`、`[凝彩]`、`[商籁]`、`[镜台]`、`[浮名]`、`[星诞]`；不写时使用常规播放器。

核心示例：
JEV:「始」请播放【星の余韻】，并启用[星诞]演出模式。「末」
JEV:「始」打开[https://example.com]。「末」
JEV:「始」对网页截图[https://example.com/page]。「末」
JEV:「始」看图[https://example.com/cat.png]。「末」
JEV:「始」请使用 {联网搜索}，从[深度学习 医疗影像 突破]和[transformer models in healthcare]这两个分级方向，搜索【AI在医疗诊断中的最新应用】。「末」
JEV:「始」请使用 {联网搜索} 中的 `谷歌学术`，在[2024年至今]的范围内搜索【大语言模型在临床诊断中的应用】。「末」
JEV:「始」请使用 {联网搜索} 中的 `B站搜索`，在B站上[搜视频]，查找【Python入门教程】。「末」
JEV:「始」请使用 {联网搜索} 中的 `B站搜索`，在B站上[搜UP主]，查找【影视飓风】。「末」
JEV:「始」请使用 {联网搜索} 中的 `B站获取`，获取视频【https://www.bilibili.com/video/BV1xx】的字幕、评论和弹幕，并在[截图 30,120 秒]指定的时间点截取高清画面。「末」
JEV:「始」主动回忆【关于上次A项目会议的讨论内容】，从[小吉的地缘政治|小吉的知识]中返回[3条]。「末」 //基于Rivermemo实现的lightmemo检索；小技巧，可以使用另一个maid的署名就能搜别人的知识库啦！
JEV:「始」主动回忆【主人喜欢吃的美食】，搜索[所有知识库]并返回[5条]。「末」//跨角色、跨日记知识库全局搜索
JEV:「始」主动回忆【VCP文坊开发日志】，限定[2026-04-11~2026-05-12]并从[Nova的知识]检索。「末」
JEV:「始」[音乐检索]【煮咖啡时适合听的歌】。「末」//语义级搜索vmusic曲目表
JEV:「始」在[1分钟后]设置闹钟，提醒我【检查烤箱里的点心】。「末」
JEV:「始」计算【integral('sin(x)', 0, pi)】。「末」
JEV:「始」联络[小娜]，告诉她【我是Nova，我想请你检查这份方案】，并使用[临时通讯]。「末」
JEV:「始」睡眠[10分钟]，醒来后提醒【重新检查异步任务状态】。「末」
JEV:「始」请使用 {图片生成}，生成【一只坐在窗边的猫，电影感】，画面需要[适合手机壁纸]。「末」
JEV:「始」请使用 {图片生成}，参考图片[file:///C:/images/person.png]，把人物修改成【雨夜霓虹风格】，并使用[2K]清晰度。「末」
JEV:「始」请使用 {图片生成} 中的 `GPT生图`，生成【一张细节丰富的科幻城市概念图】，并采用[横版高清]画幅。「末」