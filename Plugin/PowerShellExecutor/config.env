# PowerShellExecutor 插件安全配置

# --- 完全禁止的指令 ---
# 列出完全禁止的指令中的关键字。
# 如果指令的任何部分（不区分大小写）匹配到这里的任何一个关键字，执行将被阻止。
# 请用英文逗号 "," 分隔多个关键字。
# 示例: FORBIDDEN_COMMANDS=rm,del,format,rmdir,Remove-Item -Recurse -Force C:\
FORBIDDEN_COMMANDS=rm,del,format,rmdir

# --- 需要管理员授权的指令 ---
# 列出需要管理员验证码 (tool_password) 才能执行的指令中的关键字。
# 如果指令的任何部分（不区分大小写）匹配到这里的任何一个关键字，则调用时必须提供正确的 'tool_password' 参数。
# 请用英文逗号 "," 分隔多个关键字。
# 示例: AUTH_REQUIRED_COMMANDS=net,shutdown,restart,New-Item
AUTH_REQUIRED_COMMANDS=net,shutdown,restart,Set-Item,Remove-Item,Set-Content,Add-Content,Clear-Content
