# 文件类型后缀白名单，使用英文逗号分隔，大小写不敏感。
# 后缀可带或不带前导点；留空表示显示所有文件类型。
# 示例：png,jpg,jpeg,webp,gif,mp4,webm,pdf,txt,md
FILE_EXTENSION_WHITELIST=png,jpg,jpeg,webp,gif,mp4,webm,pdf,txt,md

# 每个文件夹最多显示的文件数。
# 文件按修改时间（mtime）从新到旧排列，超出数量的较旧文件不会显示。
# 此限制对每个文件夹独立生效，不限制子文件夹数量；留空表示不限制，0 表示不显示文件。
MAX_FILES_PER_DIRECTORY=